version: '3.8'
services:
  app:
    build: 
      context: ..
      dockerfile: .devcontainer/Dockerfile
    env_file:
      - ../.env
    ports:
      - "5000:5000"
    volumes:
      - ..:/app
    command: sleep infinity
    extra_hosts:
      - "host.docker.internal:host-gateway"